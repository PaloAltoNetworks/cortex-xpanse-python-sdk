name: Release and Publish Python SDK

on:
  push:
    branches:
      - main_test
  pull_request:
    branches:
      - main_test

jobs:
  build-n-publish:
    name: Create Release
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.9]

    steps:
      - uses: actions/checkout@v1
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v1
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt
          python3 -m pip install --upgrade twine
          python3 -m pip install --upgrade build
      - name: Lint with black
        run: |
          black --check xpanse/
      - name: Test types with mypy
        run: |
          mypy xpanse/**/*.py
      - name: Test with pytest
        run: |
          python -m pytest --vcr-record=none tests/
      - name: Test with whispers
        run: |
          whispers --config whispers-config.yml --rules jwtvalue,bearervalue tests
      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}
          body: |
            Changes in this Release
            - Updates to match new logo and branding
          draft: true
          prerelease: true
      - name: Build
        run: |
          python3 -m build
      - name: Upload with twine
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}
        run: |
          python3 -m twine upload -r Xpanse dist/*
      - name: Publish Package to PyPI
        run: |
          python3 -m pip install --index-url https://test.pypi.org/simple/ --no-deps Xpanse-cachukwu